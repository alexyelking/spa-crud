version: '3'

services:

  spa:
    image: node:lts-alpine
    restart: "no"
    environment:
      - API_URL=${API_URL}
    volumes:
      - ./app:/app
      - /app/node_modules
    ports:
      - ${NGINX_PORT}:8080
    working_dir: /app
    command: [sh, -c, "npm install && npm run serve"]
